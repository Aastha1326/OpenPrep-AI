/*** Focus Room Socket Handler — stub for Pomodoro focus rooms ***/
module.exports = (io) => {
  // No-op: placeholder to satisfy server.js import.
  // Real focus-room logic can be added here later.
  io.on('connection', () => {});
};
